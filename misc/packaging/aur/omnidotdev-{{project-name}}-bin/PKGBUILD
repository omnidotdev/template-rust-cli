# Maintainer: Omni <team@omni.dev>
pkgname=omnidotdev-{{project-name}}-bin
pkgver=0.1.0
pkgrel=1
pkgdesc="{{description}} (pre-built)"
arch=('x86_64')
url="https://{{binary-name}}.omni.dev"
license=('Apache-2.0')
depends=()
provides=('omnidotdev-{{project-name}}')
conflicts=('omnidotdev-{{project-name}}')
source=("$pkgname-$pkgver.tar.gz::https://github.com/omnidotdev/{{project-name}}/releases/download/v$pkgver/{{binary-name}}-x86_64-unknown-linux-gnu.tar.gz"
        "$pkgname-$pkgver-LICENSE::https://raw.githubusercontent.com/omnidotdev/{{project-name}}/v$pkgver/LICENSE.md")
sha256sums=('SKIP'
            'SKIP')

package() {
  install -Dm755 "{{binary-name}}" "$pkgdir/usr/bin/{{binary-name}}"
  install -Dm644 "$pkgname-$pkgver-LICENSE" "$pkgdir/usr/share/licenses/$pkgname/LICENSE"
}
